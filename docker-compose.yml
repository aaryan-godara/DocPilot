# ==================================================
# DocPilot — Docker Compose (Placeholder)
# ==================================================
# This file will be fully configured in Week 3.
# For now it defines the service structure.
# ==================================================

version: "3.9"

services:
  # ------------------------------------------------
  # FastAPI Backend
  # ------------------------------------------------
  backend:
    build:
      context: .
      dockerfile: Dockerfile  # TODO: Create Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    # command: uvicorn app.backend.main:app --host 0.0.0.0 --port 8000
    # depends_on:
    #   - chromadb

  # ------------------------------------------------
  # Streamlit Frontend
  # ------------------------------------------------
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend  # TODO: Create Dockerfile.frontend
    ports:
      - "8501:8501"
    env_file:
      - .env
    # command: streamlit run app/frontend/streamlit_app.py --server.port 8501

  # ------------------------------------------------
  # ChromaDB (Week 2+)
  # ------------------------------------------------
  # chromadb:
  #   image: chromadb/chroma:latest
  #   ports:
  #     - "8001:8000"
  #   volumes:
  #     - chroma_data:/chroma/chroma

# volumes:
#   chroma_data:
